#include "structs.h"

/*
 * Пространство от имена cg (Computer Graphics).
 * Тук се ДЕФИНИРАТ глобалните променливи, които бяха ДЕКЛАРИРАНИ в structs.h
 */
namespace cg {
    /*
     * ДЕФИНИЦИЯ на версията на OpenGL и GLSL.
     * Задава конкретните версии, които ще използва приложението.
     */
    Version version = {
        4,                      // OpenGL 4.x
        6,                      // OpenGL 4.6
        "#version 460"          // GLSL версия 460 (съвместима с OpenGL 4.6)
    };

    /*
     * ДЕФИНИЦИЯ на параметрите на прозореца.
     * Определя началния размер и заглавието на приложението.
     */
    Window window = {
        1280,                   // Ширина: 1280 пиксела
        720,                    // Височина: 720 пиксела (16:9 съотношение)
        "Computer Graphics"     // Заглавие на прозореца
    };

    /*
     * ДЕФИНИЦИЯ на камерата.
     * Позиционира камерата в 3D пространството и определя нейната ориентация.
     */
    Camera camera = {
        glm::vec3(0.0f, 0.0f, 3.0f),  // Позиция: (0,0,3) - 3 единици по оста Z напред
        glm::vec3(0.0f, 0.0f, 0.0f),  // Център: (0,0,0) - гледа към началото на координатната система
        glm::vec3(0.0f, 1.0f, 0.0f)   // Up вектор: (0,1,0) - положителна Y посока е "нагоре"
    };

    /*
     * ДЕФИНИЦИЯ на перспективната проекция.
     * Задава параметрите на 3D->2D проекцията.
     */
    Perspective perspective = {
        glm::radians(45.0f),    // Поле на зрение: 45° (преобразувано в радиани)
        16.0f / 9.0f,           // Съотношение на страните: 16:9 (съвпада с резолюцията 1280x720)
        0.1f,                   // Близка равнина: 0.1 единици (обекти по-близо не се виждат)
        100.0f                  // Далечна равнина: 100 единици (обекти по-далечни не се виждат)
    };

    /*
     * ДЕФИНИЦИЯ на робота.
     * Създава инстанция на робота със зададените по подразбиране стойности.
     * Всички стойности са зададени в дефиницията на структурата,
     * така че тук се създава обект с тези стойности по подразбиране.
     */
    Robot robot;
}